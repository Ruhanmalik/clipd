# Secrets — never commit. See plan.md §4.
.env

# Runtime store: clips, screenshots, thumbnails, SQLite index.
data/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# macOS
.DS_Store

# Local reference notes, not part of the repo.
clipd-explained.md

# Client config carries the ingest token; only the example is committed.
client/config.toml
client/.clipwatch/
clipwatch-explained.md
